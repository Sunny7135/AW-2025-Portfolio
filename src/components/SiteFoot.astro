<footer class="site-foot">
  <div class="site-foot__logo">
    <a href="/" style="text-decoration: none;">
      <span class="site-foot__name">SUNNY KARDAM</span>
      <span class="u-sr-only">Sunny Kardam</span>
    </a>
  </div><!-- .site-foot__logo -->
</footer>

<style lang="scss">
  .site-foot {
    @include flex(row, center, center);

    position: relative;
    z-index: 3;

    padding: 1.5rem;

    border-top: 1px solid color(secondary);

    @include mq(phone) {
      padding: 1rem;
    }

    &__logo {
      .site-foot__name {
        display: block;
        color: #160000;
        font-family: 'Bigger Display', system-ui, sans-serif;
        font-weight: 700;
        font-size: 1.75rem;
        line-height: 1;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        text-align: center;

        @include mq(phone) {
          font-size: 1.25rem;
        }
      }
    }
  }
</style>
